---
name: cross-field-search
description: Slash-command shim for /cross-field-search. Delegates to commands/cross-field-search.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /cross-field-search (command shim)

This skill exists so Claude Code can resolve **`/cross-field-search`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/cross-field-search.md`** at the repository root (or **`~/.claude/commands/cross-field-search.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
