---
name: design-novelty-check
description: Slash-command shim for /design-novelty-check. Delegates to commands/design-novelty-check.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /design-novelty-check (command shim)

This skill exists so Claude Code can resolve **`/design-novelty-check`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/design-novelty-check.md`** at the repository root (or **`~/.claude/commands/design-novelty-check.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
