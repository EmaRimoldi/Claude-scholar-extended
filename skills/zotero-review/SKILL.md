---
name: zotero-review
description: Slash-command shim for /zotero-review. Delegates to commands/zotero-review.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /zotero-review (command shim)

This skill exists so Claude Code can resolve **`/zotero-review`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/zotero-review.md`** at the repository root (or **`~/.claude/commands/zotero-review.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
