---
name: check-competition
description: Slash-command shim for /check-competition. Delegates to commands/check-competition.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /check-competition (command shim)

This skill exists so Claude Code can resolve **`/check-competition`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/check-competition.md`** at the repository root (or **`~/.claude/commands/check-competition.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
