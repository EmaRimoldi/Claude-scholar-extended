---
name: novelty-gate
description: Slash-command shim for /novelty-gate. Delegates to commands/novelty-gate.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /novelty-gate (command shim)

This skill exists so Claude Code can resolve **`/novelty-gate`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/novelty-gate.md`** at the repository root (or **`~/.claude/commands/novelty-gate.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
