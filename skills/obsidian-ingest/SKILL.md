---
name: obsidian-ingest
description: Slash-command shim for /obsidian-ingest. Delegates to commands/obsidian-ingest.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /obsidian-ingest (command shim)

This skill exists so Claude Code can resolve **`/obsidian-ingest`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/obsidian-ingest.md`** at the repository root (or **`~/.claude/commands/obsidian-ingest.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
