---
name: compile-manuscript
description: Slash-command shim for /compile-manuscript. Delegates to commands/compile-manuscript.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /compile-manuscript (command shim)

This skill exists so Claude Code can resolve **`/compile-manuscript`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/compile-manuscript.md`** at the repository root (or **`~/.claude/commands/compile-manuscript.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
