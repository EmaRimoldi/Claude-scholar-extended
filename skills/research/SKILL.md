---
name: research
description: Slash-command shim for /research. Delegates to commands/sc/research.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /research (command shim)

This skill exists so Claude Code can resolve **`/research`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/sc/research.md`** at the repository root (or **`~/.claude/commands/research.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
