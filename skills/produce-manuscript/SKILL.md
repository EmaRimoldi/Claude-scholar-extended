---
name: produce-manuscript
description: Slash-command shim for /produce-manuscript. Delegates to commands/produce-manuscript.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /produce-manuscript (command shim)

This skill exists so Claude Code can resolve **`/produce-manuscript`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/produce-manuscript.md`** at the repository root (or **`~/.claude/commands/produce-manuscript.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
