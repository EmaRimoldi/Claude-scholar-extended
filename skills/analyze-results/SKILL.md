---
name: analyze-results
description: Slash-command shim for /analyze-results. Delegates to commands/analyze-results.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /analyze-results (command shim)

This skill exists so Claude Code can resolve **`/analyze-results`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/analyze-results.md`** at the repository root (or **`~/.claude/commands/analyze-results.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
