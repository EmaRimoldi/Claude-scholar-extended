---
name: design-experiments
description: Slash-command shim for /design-experiments. Delegates to commands/design-experiments.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /design-experiments (command shim)

This skill exists so Claude Code can resolve **`/design-experiments`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/design-experiments.md`** at the repository root (or **`~/.claude/commands/design-experiments.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
