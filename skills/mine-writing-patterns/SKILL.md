---
name: mine-writing-patterns
description: Slash-command shim for /mine-writing-patterns. Delegates to commands/mine-writing-patterns.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /mine-writing-patterns (command shim)

This skill exists so Claude Code can resolve **`/mine-writing-patterns`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/mine-writing-patterns.md`** at the repository root (or **`~/.claude/commands/mine-writing-patterns.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
