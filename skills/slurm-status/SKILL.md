---
name: slurm-status
description: Slash-command shim for /slurm-status. Delegates to commands/slurm-status.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /slurm-status (command shim)

This skill exists so Claude Code can resolve **`/slurm-status`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/slurm-status.md`** at the repository root (or **`~/.claude/commands/slurm-status.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
