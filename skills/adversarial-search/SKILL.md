---
name: adversarial-search
description: Slash-command shim for /adversarial-search. Delegates to commands/adversarial-search.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /adversarial-search (command shim)

This skill exists so Claude Code can resolve **`/adversarial-search`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/adversarial-search.md`** at the repository root (or **`~/.claude/commands/adversarial-search.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
