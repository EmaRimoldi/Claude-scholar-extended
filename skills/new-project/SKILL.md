---
name: new-project
description: Slash-command shim for /new-project. Delegates to commands/new-project.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /new-project (command shim)

This skill exists so Claude Code can resolve **`/new-project`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/new-project.md`** at the repository root (or **`~/.claude/commands/new-project.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
