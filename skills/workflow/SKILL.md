---
name: workflow
description: Slash-command shim for /workflow. Delegates to commands/sc/workflow.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /workflow (command shim)

This skill exists so Claude Code can resolve **`/workflow`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/sc/workflow.md`** at the repository root (or **`~/.claude/commands/workflow.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
