---
name: claim-search
description: Slash-command shim for /claim-search. Delegates to commands/claim-search.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /claim-search (command shim)

This skill exists so Claude Code can resolve **`/claim-search`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/claim-search.md`** at the repository root (or **`~/.claude/commands/claim-search.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
