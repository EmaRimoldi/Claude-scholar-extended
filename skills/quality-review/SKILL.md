---
name: quality-review
description: Slash-command shim for /quality-review. Delegates to commands/quality-review.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /quality-review (command shim)

This skill exists so Claude Code can resolve **`/quality-review`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/quality-review.md`** at the repository root (or **`~/.claude/commands/quality-review.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
