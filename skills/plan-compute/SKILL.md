---
name: plan-compute
description: Slash-command shim for /plan-compute. Delegates to commands/plan-compute.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /plan-compute (command shim)

This skill exists so Claude Code can resolve **`/plan-compute`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/plan-compute.md`** at the repository root (or **`~/.claude/commands/plan-compute.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
