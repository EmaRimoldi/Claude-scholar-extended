---
name: brainstorm
description: Slash-command shim for /brainstorm. Delegates to commands/sc/brainstorm.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /brainstorm (command shim)

This skill exists so Claude Code can resolve **`/brainstorm`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/sc/brainstorm.md`** at the repository root (or **`~/.claude/commands/brainstorm.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
