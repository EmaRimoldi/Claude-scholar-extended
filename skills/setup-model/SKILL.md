---
name: setup-model
description: Slash-command shim for /setup-model. Delegates to commands/setup-model.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /setup-model (command shim)

This skill exists so Claude Code can resolve **`/setup-model`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/setup-model.md`** at the repository root (or **`~/.claude/commands/setup-model.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
