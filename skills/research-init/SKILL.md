---
name: research-init
description: Slash-command shim for /research-init. Delegates to commands/research-init.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /research-init (command shim)

This skill exists so Claude Code can resolve **`/research-init`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/research-init.md`** at the repository root (or **`~/.claude/commands/research-init.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
