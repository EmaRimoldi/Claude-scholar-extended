---
name: run-experiment
description: Slash-command shim for /run-experiment. Delegates to commands/run-experiment.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /run-experiment (command shim)

This skill exists so Claude Code can resolve **`/run-experiment`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/run-experiment.md`** at the repository root (or **`~/.claude/commands/run-experiment.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
