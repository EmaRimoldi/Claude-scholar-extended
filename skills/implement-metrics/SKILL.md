---
name: implement-metrics
description: Slash-command shim for /implement-metrics. Delegates to commands/implement-metrics.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /implement-metrics (command shim)

This skill exists so Claude Code can resolve **`/implement-metrics`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/implement-metrics.md`** at the repository root (or **`~/.claude/commands/implement-metrics.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
