---
name: zotero-notes
description: Slash-command shim for /zotero-notes. Delegates to commands/zotero-notes.md; use when the client resolves this command via the Skill tool.
version: 0.1.0
tags: [SlashCommand, ClaudeCode, ALETHEIA]
command_shim: true
---

# /zotero-notes (command shim)

This skill exists so Claude Code can resolve **`/zotero-notes`** when slash commands are routed through the Skill tool.

**Do this:** Read and follow **`commands/zotero-notes.md`** at the repository root (or **`~/.claude/commands/zotero-notes.md`** after `bash scripts/setup.sh`), then execute that specification.

<!-- command-skill-shim generated; safe to regenerate with scripts/sync_command_skill_shims.py -->
