# @package _global_
# 2x2x2 ablation condition: target=selective transform=softmax loss=kl
model:
  attention_transform: softmax
  supervised_heads: "top_k"
  lambda_attn: 1.0
  attn_loss_type: kl
  top_k: 24
