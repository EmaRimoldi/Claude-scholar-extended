# @package _global_
# 2x2x2 ablation condition: target=selective transform=softmax loss=mse
model:
  attention_transform: softmax
  supervised_heads: "top_k"
  lambda_attn: 1.0
  attn_loss_type: mse
  top_k: 24
